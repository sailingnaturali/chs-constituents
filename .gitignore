node_modules/
dist/
__pycache__/
.cache/

# CHS-derived output — never redistribute (see README)
currents.json
*.bundle.json
.venv/
.DS_Store

# Reference-tide fragment for the offline engine — CHS-derived, operator licence,
# NOT for redistribution (same stance as currents.json above).
currents-chs.json
